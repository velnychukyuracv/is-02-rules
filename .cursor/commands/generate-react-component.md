---
description: "Generate a React component following project conventions"
---

Create a new React component with these requirements:

- Name: $ARGUMENTS
- Use functional component with hooks
- Define `{Name}Props` interface
- Use named export (not default)
- Create colocated test file `{Name}.test.tsx`
- Follow existing patterns in the project

Use the project's established conventions from `.cursor/rules/`.

